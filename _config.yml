# ---------------------------------------------------------------
#  Site-wide settings. Change a value here and it updates
#  everywhere it appears across the site.
# ---------------------------------------------------------------
title: Niv Ginat
tagline: Product & Brand Design
description: >-
  Product and brand designer. Fifteen years designing the moments
  where people decide to commit.
url: https://nivginat.com

# --- contact + social: edit once, updates every page ---
author:
  name: Niv Ginat
  email: niv.ginat@gmail.com
  phone_display: "+1 818 726 9932"
  phone_href: "+18187269932"

social:
  behance: https://www.behance.net/nivginatc07e

# --- footer ---
copyright_start: 2019

# --- third-party versions, in one place ---
bootstrap_version: 5.3.3

# --- projects collection ---
# Extensionless URLs (/projects/telltail). Jekyll still writes the file as
# telltail.html, and GitHub Pages serves it at both addresses, so links
# already out in the world keep working.
collections:
  projects:
    output: true
    permalink: /projects/:name

defaults:
  - scope: { path: "", type: "projects" }
    values: { layout: "project" }

# --- work-grid filter buttons ---
filters:
  - { label: All,       value: "*" }
  - { label: "Brand & Identity", value: ".branding" }
  - { label: Packaging, value: ".packaging" }
  - { label: Print,     value: ".print" }
  - { label: "Web & Product", value: ".ux_ui" }

# --- client strip --------------------------------------------------------
# A row without a `logo` falls back to the name set in the site's own face —
# honest, and better than a mark we had to invent. Every row carries a real
# one now; the fallback is there for the next client.
# `scale` trims the odd mark that still reads heavy or light once the strip
# has height-matched it — a lockup with a second line (9Yards) loses height
# to it and needs winding back up.
# Ordered by how quickly a cold visitor recognises the name, not
# chronologically — the first two do the most work.
clients:
  - { name: "AT&T", logo: "att.svg", scale: 1.12 }
  - { name: "AMC", logo: "amc.svg", scale: 1.25 }
  - { name: "UCLA", logo: "ucla.svg", scale: 0.95 }
  - { name: "APM Music", logo: "apm-music.svg", scale: 1.05 }
  - { name: "ClimateCheck", logo: "climatecheck.svg", scale: 0.554 }
  - { name: "Modern Life", logo: "modern-life.svg", scale: 0.75 }
  - { name: "FastPay", logo: "fastpay.svg", scale: 1.43 }
  - { name: "9Yards Capital", logo: "9yards-capital.svg", scale: 1.25 }
  - { name: "True Capital", logo: "true-capital.svg", scale: 0.85 }
  - { name: "Lightdrop", logo: "lightdrop.svg", scale: 0.89 }

# --- analytics -----------------------------------------------------------
# Paste the GA4 Measurement ID here (looks like G-XXXXXXXXXX) and the tag
# loads on every page. Leave empty and nothing is injected at all — no
# script, no cookie, no consent banner needed.
google_analytics: ""

# Search Console's "HTML tag" verification value — the content="..." string
# only, not the whole meta tag. Can be removed once verified; keeping it
# means re-verification never breaks.
google_site_verification: ""

exclude: [README.md, NOTES.md, Gemfile, Gemfile.lock, vendor]
